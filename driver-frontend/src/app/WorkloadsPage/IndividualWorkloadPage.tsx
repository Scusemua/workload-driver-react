import { Workload } from '@src/Data';

interface IndividualWorkloadPageProps {
  workload: Workload;
}

const IndividualWorkloadPage: React.FunctionComponent<IndividualWorkloadPageProps> = (props: IndividualWorkloadPageProps) => {
  return (<></>)
}
